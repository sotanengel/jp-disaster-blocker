name: feature_hazard
description: Hazard layer overlay (PMTiles) for jp-disaster-blocker.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

resolution: workspace

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  flutter_map: ^8.0.0
  flutter_map_pmtiles: ^1.0.0
  latlong2: ^0.9.0
  pmtiles: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  assets:
    - assets/hazard/
