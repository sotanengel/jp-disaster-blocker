name: feature_routing
description: Walking route feature for jp-disaster-blocker.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

resolution: workspace

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  infra_routing_engine:
    path: ../infra_routing_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0
