name: core
description: Core types, Result, exceptions, Logger, and EventBus with zero dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^7.0.0
