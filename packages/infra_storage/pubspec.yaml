name: infra_storage
description: SQLite / R-Tree / sqlite-vec wrapper for jp-disaster-blocker.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

resolution: workspace

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  path: ^1.9.0
  sqflite: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.4.0
  very_good_analysis: ^7.0.0
